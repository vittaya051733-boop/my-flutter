rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Unrestricted access (dev/testing only)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
